#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@pepperi-addons/scripts/create.js" "$@"
  ret=$?
else 
  node  "$basedir/../@pepperi-addons/scripts/create.js" "$@"
  ret=$?
fi
exit $ret
